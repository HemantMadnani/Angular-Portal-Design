import { ChangeCursorDirective } from './change-cursor.directive';

describe('ChangeCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeCursorDirective();
    expect(directive).toBeTruthy();
  });
});
